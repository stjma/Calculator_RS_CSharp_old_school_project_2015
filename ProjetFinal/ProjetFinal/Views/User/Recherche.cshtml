
@{
    ViewBag.Title = "Recherche";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Recherche</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>skill</h4>
        <hr />

        @Html.Hidden(name:"idRecherche", value:ViewContext.RouteData.Values["id"])

        <div class="form-group">
            @Html.Label("Xp que vous avez", "id_xptable", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
              @*@Html.TextArea(name:"xpActuel", value:"", htmlAttributes:new { htmlAttributes = new { @class = "form-control", @type = "number" } })*@

                
                <input type="number" name="lvlActuel" />
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Niveau que vous voulez", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @*@Html.TextArea(name:"lvlVoulu", value:"", htmlAttributes: new { htmlAttributes = new { @class = "form-control", @type = "number" } })*@
                <input type="number" name="xpVoulu"/>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
 }

<div>
    @Html.ActionLink("Back to List", "AllSkill")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}



@*@Html.ActionLink("Delete", "DeleteXp", new { idXp = 1,  id = Recherche.GetElementById("NextAction").value,  xp = 1 ,  lvl = 1 }, new { @class = "btn btn-info" })*@
